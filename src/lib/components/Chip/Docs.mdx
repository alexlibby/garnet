import { Canvas, Meta } from "@storybook/addon-docs";
import { ArgTypes } from "@storybook/blocks";
import * as ChipStories from "./Chip.stories";

<Meta of={ChipStories} />

# Chip

Button is the primary component. It has three possible states.

- [Default](#default)
- [CloseButton](#closebutton)
- [Outline](#outline)

## Default

This is the default state for the Chip component:

<Canvas of={ChipStories.Default} />

## CloseButton

This shows the button with a close option:

<Canvas of={ChipStories.CloseButton} />

## Outline

This variant shows the Chip with an outline style:

<Canvas of={ChipStories.Outline} />

## Properties of component

Below is a list of arguments available for this component:

<ArgTypes of={ChipStories} />
