import { Canvas, Meta } from "@storybook/addon-docs";
import { ArgTypes } from "@storybook/blocks";
import * as CheckboxStories from "./Checkbox.stories";

<Meta of={CheckboxStories} />

# Checkbox

Checkbox is the primary component. It has two possible states.

- [Default](#default)
- [Disabled](#disabled)

<a id="default" />

## Default

This is the default version of the Checkbox component.

<Canvas of={CheckboxStories.Default} />

<a id="default" />

## Disabled

This version shows a Checkbox component in a disabled state.

<Canvas of={CheckboxStories.Disabled} />

## Properties of component

Below is a list of arguments available for this component:

<ArgTypes of={CheckboxStories} />
